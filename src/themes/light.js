import {DefaultTheme} from '@react-navigation/native'

const lightTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
      background: '#fff',
    },
  };

export default lightTheme